@model LoginViewModel

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Bills) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DueDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Citizen.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Settlement.Id)
            </td>
            <td>
                <a asp-action="SelectSettlement" asp-route-id="@item.Id">Settlement</a> |
				<a asp-action="SelectPayment" asp-route-id="@item.Id">Payment</a> |
            </td>
        </tr>
}
    </tbody>
</table>
